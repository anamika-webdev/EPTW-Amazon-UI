
  # Enterprise PTW Dashboard UI

  This is a code bundle for Enterprise PTW Dashboard UI. The original project is available at https://www.figma.com/design/DOEmzOS1x7CvIRUeH8pOMf/Enterprise-PTW-Dashboard-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  